@model IEnumerable<bugtracker.Models.Bug>
@{
    ViewData["Title"] = "Inicio";
}

<div class="container-flex">
    
    <div class="box-1">
         <h4>Bem Vindo!</h4><br>
            <p class="p-welcome">
                Obrigado por visitar o <strong>Bugtracker</strong>. Bugtracker é um sistema de rastreamento de bugs usado para enviar e rever defeitos que foram encontrados em sistemas de software. Bugtracker não é um sistema para assistência técnica ou suporte, mas simplesmente um sistema de rastreamento de bugs. Se você enviar um defeito, por favor, forneça informações detalhadas em sua apresentação depois de ter consultado o Bugtracker para garantir que o defeito já não tenha sido relatado. 
            </p>
    </div>

    <div class="box-2">
        <img src="Assets/Images/bug-banner.png" alt="Picture of a bug">
    </div>      

</div>

<div class="indexBugSpacing"></div>

<div>
            <h5>Bugs adicionados recentemente</h5><br>
                <table class="table">
                    <tr>
                        <th>
                            @Html.DisplayNameFor(model => model.Title)
                        </th>
                        <th>
                            @Html.DisplayNameFor(model => model.Description)
                        </th>
                        <th>
                            @Html.DisplayNameFor(model => model.Product)
                        </th>
                        <th>
                            @Html.DisplayNameFor(model => model.Version)
                        </th>
                        <th>
                            @Html.DisplayNameFor(model => model.Status)
                        </th>
                        <th>
                            @Html.DisplayNameFor(model => model.Type)
                        </th>
                    </tr>

                    @if (Model != null)
                    {
                        @foreach (var item in Model)
                        {
                            <tr>
                                <td>
                                    @Html.DisplayFor(modelItem => item.Title)
                                </td>
                                <td>
                                    @Html.DisplayFor(modelItem => item.Description)
                                </td>
                                <td>
                                    @Html.DisplayFor(modelItem => item.Product)
                                </td>
                                <td>
                                    @Html.DisplayFor(modelItem => item.Version)
                                </td>
                                <td>
                                    @Html.DisplayFor(modelItem => item.Status)
                                </td>
                                <td>
                                    @Html.DisplayFor(modelItem => item.Type)
                                </td>
                            </tr>
                        }
                    }
                </table>
        </div>